[project]
name = "flagbounce"
version = "0.1.0"
description = "Country Marbles Flag Race Game"
requires-python = ">=3.8"
dependencies = [
    "flask>=3.0.0",
    "flask-cors>=4.0.0",
    "playwright>=1.48.0",
]
